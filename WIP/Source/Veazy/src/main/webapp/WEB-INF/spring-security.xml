<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security-4.1.xsd">

    <http auto-config="true" use-expressions="true" create-session="always" >
        <!-- <intercept-url pattern="/**" access="hasAnyRole('1','2','3','4')" />
        <intercept-url pattern="/api/editorcp**" access="hasRole('2')" />
        <intercept-url pattern="/api/cmcp**" access="hasRole('3')" />
        <intercept-url pattern="/api/admincp**" access="hasRole('4')" /> -->
        <form-login 
            login-page="/login" 
            default-target-url="/" 
            authentication-failure-url="/login?error" 
            username-parameter="username"
            password-parameter="password" />
        <logout logout-success-url="/login?logout" />
        <session-management session-fixation-protection="migrateSession"/>
        <!-- <remember-me data-source-ref="dataSource" token-validity-seconds="86400"/> 1 day -->
        <csrf disabled="true" />
    </http>

    <authentication-manager>
        <authentication-provider ref="veazyAuthenProvider" />
    </authentication-manager>

</beans:beans>